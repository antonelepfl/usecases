image: node:latest

pages:
  cache:
    paths:
      - node_modules/
  before_script:
    - npm install
  stage: deploy
  script:
  - rm -rdf docs
  - npm run firebase
  artifacts:
    paths:
    - docs
  only:
    - dev
